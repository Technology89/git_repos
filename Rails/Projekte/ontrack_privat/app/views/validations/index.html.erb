<p id="notice"><%= notice %></p>

<h1>Listing Validations</h1>

<p>Ranking: <%= create_ranking(@project) %></p>

<table>
  <thead>
    <tr>
      <th>Comment</th>
      <th>Username</th>
      <th>Ranking</th>
      <th>Bewertung</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @validations.each do |validation| %>
      <tr>
        <td><%= validation.comment %></td>
        <td><%= validation.username %></td>
        <td><%= show_stars(validation) %></td>
        <td><%= create_text(validation) %></td>
        <td><%= link_to 'Show', project_validation_path(@project, validation) %></td>
        <td><%= link_to 'Edit', edit_project_validation_path(@project, validation) %></td>
        <td><%= link_to 'Destroy', project_validation_path(@project, validation), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Validation', new_project_validation_path(@project) %>
