<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if current_user == @user %>
	<%= link_to 'Edit', edit_user_path(@user) %> |
	<%= link_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end -%>

<% if @user.reviews.any? %>
	<h2>Registriert für:</h2>
		<ul>
			<% @user.reviews.each do |review| %>
				<li><%= link_to review.movie.title, review.movie %></li>
			<% end -%>
		</ul>
<% end -%>
<% if @favorite_movies.any? %>
	<h3>Favorisierte Events</h3>
	<ul>
		<% @favorite_movies.each do |var| %>
			<li><%=link_to var.title, var %></li>
		<% end %>
	</ul>
<% end %>

<%= link_to 'Back', users_path %>