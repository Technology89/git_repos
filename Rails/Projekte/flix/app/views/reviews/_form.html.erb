<%= render "shared/errors", object: @review %>
<%=	form_for [@movie, @review] do |f| %>
	<div class="row">
			<div class="medium-3 columns">
				<%= f.label :stars %>
			</div>
			<div class="medium-9 columns">
				<%= f.select :stars, Review::RATING, prompt: "Bitte wählen!" %>
			</div>
		</div>	
			<div class="row">
			<div class="medium-3 columns">
				<%= f.label :comment %>
			</div>
			<div class="medium-9 columns">
				<%= f.text_field :comment, prompt: "Bitte bewerten Sie den Film!" %>
			</div>
		</div>	
			<div class="row">
			<div class="medium-12 columns">
				<%= f.submit %>
			</div>
		</div>	
<% end %>